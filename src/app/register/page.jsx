import RegisterPage from '@/componants/RegisterPage';
export const metadata = {
  title: 'Register',
};

const page = () => {
  return (
    <div>
      <RegisterPage/>
    </div>
  )
}

export default page